Here should be located classes which serve Expenses controller.
All classes should describe only one api unless interface will
be changed to call different methods based on the input params.
All classes should inherit common interface of the api.

